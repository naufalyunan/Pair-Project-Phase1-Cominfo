<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form id="form" method="POST" action= <%= `/traders/edit/${trader.id}` %> >
        <div class="form-group">
          <label for="name">Name</label>
          <input type="text" name="name" class="form-control" id="inputAddress"  value = <%= trader.name %> required>
          <label for="birthdate">Birthdate</label>
          <input type="date" name="birthdate" class="form-control" id="inputAddress" value = <%= trader.birthdate %> required>
          <input type="text" name="username" class="form-control" id="inputAddress" value = <%= trader.username %>  required>
          <label for="password">Password</label>
          <input type="password" name="password" class="form-control" id="inputAddress" value = <%= trader.password %>  required>
        </div>
        <button type="submit" class="btn btn-primary">Save</button> <a href="/" type="submit" class="btn btn-danger">Cancel</a>
    </form>
</body>
</html>